apiVersion: batch/v1
kind: Job
metadata:
  name: node-poc-job
  namespace: default
spec:
  template:
    spec:
      containers:
      - args:
        - /bin/sh
        - -c
        - date; echo "Starting CronJob"; ansible --version; ansible-playbook create-ec2.yml; echo "Finished"; exit 0
        image: $imageName
        envFrom:
        - secretRef:
            name: kt-aws-creds
        imagePullPolicy: Always
        name: node-poc-job
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      imagePullSecrets:
      - name: afour-private-registry-creds
      restartPolicy: Never
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
